<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:WPFVisualisatie"
    x:Class="WPFVisualisatie.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="1000" Width="1000">
    <Window.DataContext>
        <local:MainWindowDataContext/>
    </Window.DataContext>
    <Grid Background="LightBlue">
        
        <DockPanel>
            <Menu DockPanel.Dock="Top" Height="20" VerticalAlignment="Top" Background="Gray">
                <MenuItem Header="Menu">
                    <MenuItem Header="OpenCurrentRaceStats" Click="MenuItem_OpenCurrentRaceStats_Click" />
                    <MenuItem Header="OpenCompetitionStats" Click="MenuItem_OpenCompetitionStats_Click" />
                    <MenuItem Header="CloseApplication" Click="MenuItem_Exit_Click" />
                </MenuItem>
            </Menu>
        </DockPanel>
        <Label x:Name="LblTrackName" Content="{Binding TrackName, Mode=OneWay}" Width="175" DockPanel.Dock="Left"/>
        <Image x:Name="TrackImage" />
    </Grid>
</Window>